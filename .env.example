# Leave unset (or empty) for root deploy, e.g. custom domain at https://example.com/
# Use a path prefix only for GitHub project pages, e.g. NEXT_PUBLIC_BASE_PATH=/repo-name
# Never set to "/" alone — Next.js rejects that as basePath.
NEXT_PUBLIC_SITE_URL=https://mattchaffe.uk
